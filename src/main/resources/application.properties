spring.application.name=${APP_NAME:task}

# Server Configuration
server.port=${SERVER_PORT:8080}

# MongoDB Configuration
spring.data.mongodb.host=${MONGODB_HOST:localhost}
spring.data.mongodb.port=${MONGODB_PORT:27017}
spring.data.mongodb.database=${MONGODB_DATABASE:taskdb}

# MongoDB Authentication (solo si es necesario)
# Descomenta estas líneas si tu MongoDB requiere autenticación
#spring.data.mongodb.username=${MONGODB_USERNAME}
#spring.data.mongodb.password=${MONGODB_PASSWORD}
#spring.data.mongodb.authentication-database=${MONGODB_AUTH_DATABASE:admin}
