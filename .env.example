# Ejemplo de variables de entorno
# Copia este archivo a .env para desarrollo local

# MongoDB Configuration (sin autenticación para desarrollo local)
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_DATABASE=taskdb

# MongoDB Authentication (solo si tu MongoDB local requiere autenticación)
# Descomenta estas líneas si es necesario:
#MONGODB_USERNAME=taskdb
#MONGODB_PASSWORD=taskdb
#MONGODB_AUTH_DATABASE=admin

# Server Configuration
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=dev

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Application Configuration
APP_NAME=TaskFlow API
